[project]
name = "rebalancing"
version = "0.1.0"
description = "USDT-M futures regime-based allocation and rebalancing engine"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.2,<4",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
